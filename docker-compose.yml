services:
  trip:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: trip-app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    volumes:
      - ./data:/app/data
    restart: unless-stopped
